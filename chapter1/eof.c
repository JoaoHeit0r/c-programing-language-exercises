/*printar o valor de EOF*/
#include <stdio.h>

int main(){
	printf("%d", EOF);
}
/*notas do livro:
*EOF is an integer defined in <stdio.h>, but the specific numeric value doesn't matter as long as it is
*not the same as any char value.
*/
